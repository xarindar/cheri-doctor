# Metro Project dependencies
# Generated from working environment (Python 3.13, CUDA 12.8)

# ── Core chat / API ───────────────────────────────────────────────────────
anthropic==0.83.0
fastapi==0.129.0
uvicorn==0.41.0
pydantic==2.11.7
python-dotenv==1.2.1
PyYAML==6.0.3
openai==1.93.1

# ── RAG / retrieval ───────────────────────────────────────────────────────
numpy==2.3.5
rank-bm25==0.2.2
scikit-learn==1.8.0
sentence-transformers==5.2.3
transformers==4.57.6

# ── Vision / document processing ─────────────────────────────────────────
PyMuPDF==1.27.1
pillow==12.0.0
opencv-contrib-python-headless==4.13.0.92
pytesseract==0.3.13
scikit-image==0.26.0

# ── PyTorch (CUDA 12.8) ───────────────────────────────────────────────────
# NOTE: torch/torchvision are NOT on PyPI with CUDA suffixes.
# Install separately BEFORE running pip install -r requirements.txt:
#
#   pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128
#
torch==2.10.0+cu128
torchvision==0.25.0+cu128
